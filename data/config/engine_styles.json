{
    "EHM_TEST":{
        #"mode":"QUAD_STRIP", # PARTICLES or QUAD_STRIP, determines which params are used
        #"engineColor":[100,165,255,255],
        #"contrailDuration":2,
        #"contrailWidthMult":3,
        #"contrailWidthAddedFractionAtEnd":3,  # can be negative. makes trail spread out/narrow at the end
        #"contrailMinSeg":10, # min segment length, in pixels
        #"contrailMaxSpeedMult":0.5f,
        #"contrailAngularVelocityMult":1f,
        #"contrailColor":[100,165,255,50],
        #"type":"GLOW" # GLOW or SMOKE; additive or regular blend mode
                        
        "engineColor":[255,255,255,255],
        "contrailParticleSizeMult":4,
        "contrailParticleDuration":1,
        "contrailParticleFinalSizeMult":2.5,
        "contrailMaxSpeedMult":0.5f,
        "contrailAngularVelocityMult":0f,
        "contrailColor":[255,255,255,25],
        "contrailCampaignColor":[255,255,255,75],
        "type":"GLOW" # GLOW or SMOKE
    },
    "EHM_TEST2":{
        #"mode":"QUAD_STRIP", # PARTICLES or QUAD_STRIP, determines which params are used
        #"engineColor":[100,165,255,255],
        #"contrailDuration":2,
        #"contrailWidthMult":3,
        #"contrailWidthAddedFractionAtEnd":3,  # can be negative. makes trail spread out/narrow at the end
        #"contrailMinSeg":10, # min segment length, in pixels
        #"contrailMaxSpeedMult":0.5f,
        #"contrailAngularVelocityMult":1f,
        #"contrailColor":[100,165,255,50],
        #"type":"GLOW" # GLOW or SMOKE; additive or regular blend mode
                        
        "engineColor":[255,0,0,255],
        "contrailParticleSizeMult":4,
        "contrailParticleDuration":1,
        "contrailParticleFinalSizeMult":2.5,
        "contrailMaxSpeedMult":0.5f,
        "contrailAngularVelocityMult":0f,
        "contrailColor":[255,0,0,25],
        "contrailCampaignColor":[255,0,0,75],
        "type":"GLOW" # GLOW or SMOKE
    },
    "EHM_TEST3":{
        #"mode":"QUAD_STRIP", # PARTICLES or QUAD_STRIP, determines which params are used
        #"engineColor":[100,165,255,255],
        #"contrailDuration":2,
        #"contrailWidthMult":3,
        #"contrailWidthAddedFractionAtEnd":3,  # can be negative. makes trail spread out/narrow at the end
        #"contrailMinSeg":10, # min segment length, in pixels
        #"contrailMaxSpeedMult":0.5f,
        #"contrailAngularVelocityMult":1f,
        #"contrailColor":[100,165,255,50],
        #"type":"GLOW" # GLOW or SMOKE; additive or regular blend mode
                        
        "engineColor":[0,0,255,255],
        "contrailParticleSizeMult":4,
        "contrailParticleDuration":1,
        "contrailParticleFinalSizeMult":2.5,
        "contrailMaxSpeedMult":0.5f,
        "contrailAngularVelocityMult":0f,
        "contrailColor":[0,0,255,25],
        "contrailCampaignColor":[0,0,255,75],
        "type":"GLOW" # GLOW or SMOKE
    },
    "EHM_TEST4":{
        #"mode":"QUAD_STRIP", # PARTICLES or QUAD_STRIP, determines which params are used
        #"engineColor":[100,165,255,255],
        #"contrailDuration":2,
        #"contrailWidthMult":3,
        #"contrailWidthAddedFractionAtEnd":3,  # can be negative. makes trail spread out/narrow at the end
        #"contrailMinSeg":10, # min segment length, in pixels
        #"contrailMaxSpeedMult":0.5f,
        #"contrailAngularVelocityMult":1f,
        #"contrailColor":[100,165,255,50],
        #"type":"GLOW" # GLOW or SMOKE; additive or regular blend mode
                        
        "engineColor":[0,255,0,255],
        "contrailParticleSizeMult":4,
        "contrailParticleDuration":1,
        "contrailParticleFinalSizeMult":2.5,
        "contrailMaxSpeedMult":0.5f,
        "contrailAngularVelocityMult":0f,
        "contrailColor":[0,255,0,25],
        "contrailCampaignColor":[0,255,0,75],
        "type":"GLOW" # GLOW or SMOKE
    },
}